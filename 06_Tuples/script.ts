let user: [number, string, boolean];

///// its take only three element as same data type not other
user = [123, "deepak", true] //// it allow us
// user = [123, "deepak", true, 12] //// it not allow us add extra element in array

let color: [number, number, number];
color = [255, 255, 255]
// color = [255, 255, 255, 0.5] //////// not allows
